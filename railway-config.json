{
  "gateway": {
    "mode": "local",
    "bind": "lan",
    "auth": {
      "mode": "token",
      "token": "${OPENCLAW_GATEWAY_TOKEN}"
    },
    "controlUi": {
      "dangerouslyDisableDeviceAuth": true
    },
    "http": {
      "endpoints": {
        "chatCompletions": {
          "enabled": true
        }
      }
    }
  },
  "models": {
    "providers": {}
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "openrouter/moonshotai/kimi-k2.5"
      }
    }
  },
  "plugins": {
    "load": {
      "paths": ["/app/extensions/drive-playground", "/app/extensions/code-bridge"]
    },
    "slots": {
      "memory": "openclaw-supermemory"
    },
    "entries": {
      "openclaw-supermemory": {
        "enabled": true,
        "config": {
          "apiKey": "${SUPERMEMORY_OPENCLAW_API_KEY}"
        }
      },
      "drive-playground": {
        "enabled": true,
        "config": {
          "baseUrl": "${DRIVE_PLAYGROUND_BASE_URL}",
          "apiKey": "${DRIVE_PLAYGROUND_API_KEY}"
        }
      },
      "code-bridge": {
        "enabled": true,
        "config": {
          "baseUrl": "${CODE_BRIDGE_BASE_URL}",
          "apiKey": "${CODE_BRIDGE_API_KEY}"
        }
      }
    }
  },
  "tools": {
    "allow": [
      "drive_playground_list",
      "drive_playground_read",
      "drive_playground_write",
      "code_bridge_list",
      "code_bridge_read",
      "code_bridge_write",
      "code_bridge_run"
    ]
  },
  "channels": {
    "discord": {
      "enabled": true,
      "token": "${DISCORD_BOT_TOKEN}"
    },
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}",
      "dmPolicy": "allowlist",
      "allowFrom": ["${TELEGRAM_USER_ID}"]
    },
    "whatsapp": {
      "selfChatMode": true,
      "dmPolicy": "allowlist",
      "allowFrom": ["${USER_NUMBER}"]
    }
  }
}
